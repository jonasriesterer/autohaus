info:
  name: Vorhandene ID 1002
  type: http
  seq: 6

http:
  method: GET
  url: "{{restUrl}}/:id"
  headers:
    - name: Accept
      value: application/json
  params:
    - name: id
      value: "1002"
      type: path
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.headers['content-type']
      operator: eq
      value: application/json
    - expression: res.body.id
      operator: eq
      value: "1002"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
