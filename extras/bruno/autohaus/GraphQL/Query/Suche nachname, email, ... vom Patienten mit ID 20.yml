info:
  name: Suche nachname, email, ... vom Patienten mit ID 20
  type: graphql
  seq: 1

graphql:
  method: POST
  url: "{{graphqlUrl}}"
  headers:
    - name: ""
      value: ""
      disabled: true
  body:
    query: |-
      query ($patientId: ID! = "20") {
        patient: autohaus(autohausId: $patientId) {
          id
          version
          name
          username
          email
          anzahlFahrzeuge
          gruendungsdatum
          homepage
          telefonnummer
          adresse {
            plz
            ort
            land
          }
          autos {
            kennzeichen
            marke
            modell
            baujahr
          }
        }
      }
    variables: |-
      {
        "patientId": "20"
      }
  auth: inherit

runtime:
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.headers['content-type']
      operator: eq
      value: application/json
    - expression: res.body.data.patient
      operator: isJson

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
