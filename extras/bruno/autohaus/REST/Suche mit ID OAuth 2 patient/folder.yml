info:
  name: Suche mit ID OAuth 2 patient
  type: folder
  seq: 1

request:
  auth:
    type: oauth2
    flow: resource_owner_password_credentials
    accessTokenUrl: "{{oidcTokenUrl}}"
    credentials:
      clientId: "{{clientId}}"
      clientSecret: "{{clientSecret}}"
      placement: body
    resourceOwner:
      username: autohaus_karlsruhe
      password: p
    tokenConfig:
      id: credentials
      placement:
        header: Bearer
      source: access_token
    settings:
      autoFetchToken: true
      autoRefreshToken: false
  variables:
    - name: clientId
      value: python-client
    - name: clientSecret
      value: 1ZrjDc99bHka9wjfXMa42NodHIxvjoOB
